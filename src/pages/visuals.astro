---
// 科 · 复利效应：四化专属板块（可视化）
import BaseLayout from "../layouts/BaseLayout.astro";

const base = import.meta.env.BASE_URL.replace(/\/$/, "");
---

<BaseLayout title="科 · 复利效应 —— 商陆有四化">
  <div class="page-head">
    <span class="sec-no">科</span>
    <h1>复利效应</h1>
    <p>让每一次努力自己生长——数据与图形的复利</p>
  </div>

  <!-- 定位说明 -->
  <section class="intro">
    <p>
      紫微斗数里，「科」是四化之一，代表名声、才华与条理。
      在这里，「科」是我的第三化：<strong>让积累被看见</strong>——用数据与图形，
      把看不见的复利画出来。
    </p>
    <p>
      这个板块用数据讲故事：交互图表、探索性数据作品、可视化实验。
      它是网站最炫酷的角落，也是「认知积累」的另一种表达。
    </p>
  </section>

  <!-- 主题方向 -->
  <section class="section">
    <div class="section-head">
      <h2>这里将收录</h2>
      <p>可视化作品的四个方向，作品会慢慢长出来</p>
    </div>
    <div class="grid">
      <div class="card">
        <h3>数据故事</h3>
        <p>一组数据，讲清一个道理</p>
      </div>
      <div class="card">
        <h3>交互探索</h3>
        <p>让访客自己动手，发现答案</p>
      </div>
      <div class="card">
        <h3>认知数据</h3>
        <p>关于学习、积累与成长的数据</p>
      </div>
      <div class="card">
        <h3>趣味实验</h3>
        <p>奇奇怪怪的可视化尝试</p>
      </div>
    </div>
  </section>

  <div class="placeholder">
    <p>🚧 第一个可视化作品正在制作中</p>
    <p style="margin-top: 0.5rem;">想法：把「认知积累」做成可交互的数据故事</p>
  </div>
</BaseLayout>

<style>
  /* 板块大字标识：科（98px） */
  .page-head .sec-no {
    font-size: 92px;
    font-family: var(--font-serif);
    font-weight: 700;
    line-height: 1.1;
    color: var(--accent);
    letter-spacing: 0.02em;
    margin-bottom: 1.2rem;
  }

  .intro {
    padding: 1.2rem 0 1.2rem 1.6rem;
    border-left: 2px solid var(--accent);
    max-width: 42em;
  }

  .intro p {
    color: var(--muted);
    font-size: 1.02rem;
    line-height: 2.05;
  }

  .intro p + p {
    margin-top: 0.8rem;
  }

  .intro strong {
    color: var(--text);
    font-weight: 600;
  }

  .grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(200px, 1fr));
    gap: 1.1rem;
  }
</style>
