// @ts-check
import { defineConfig } from 'astro/config';

// https://astro.build/config
// GitHub Pages 部署配置：站点地址 + 项目路径前缀
export default defineConfig({
  site: 'https://chen-master-zw.github.io',
  base: '/shanglu-site/',
});
